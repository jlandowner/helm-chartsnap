# This file define common behavior of the chart snapshots in the test directory.
dynamicFields:
  - apiVersion: v1
    kind: Secret
    name: app1-cert
    jsonPath:
      - /data/ca.crt
      - /data/tls.crt
      - /data/tls.key
